# Crypto 5M Simple Toobit Scalper

ربات اسکالپ ۵ دقیقه‌ای برای تحلیل با OKX و اجرای واقعی روی Toobit.

## قوانین اصلی

- تحلیل فقط از OKX انجام می‌شود.
- اجرای واقعی فقط روی Toobit انجام می‌شود.
- پنل ترید، دستورات، مارجین، لوریج، آمار، سیگنال عادی/Real، اسلات‌ها و ثبت نتایج حفظ شده‌اند.
- تایم‌فریم جهت: `4H + 1H` باید همسو باشند.
- تایم‌فریم ورود: `5m`.
- بدون حمایت/مقاومت.
- بدون AI، DCA، Martingale و trailing.
- امتیاز از ۱۰۰؛ حداقل صدور سیگنال `70`.
- RR پیش‌فرض `1.5`.
- TP و SL فقط مخصوص ۵ دقیقه هستند.
- حداقل سود خالص بعد از کارمزد باید حداقل مقدار پنل باشد؛ پیش‌فرض `0.01 USDT`.

## ورود جدید: Compression Breakout Entry

این نسخه منطق پولبک را حذف کرده و ورود را بر اساس «شروع حرکت معتبر بعد از فشردگی قیمت» انجام می‌دهد.

ربات دیگر صرفاً به این دلیل که EMA/RSI/MACD هم‌جهت هستند وارد نمی‌شود. اول 4H و 1H جهت را مشخص می‌کنند؛ بعد در 5M فقط وقتی ورود می‌دهد که قیمت از یک محدوده فشرده کوتاه خارج شده باشد.

### قوانین ورود LONG

- 4H و 1H صعودی باشند.
- ۸ کندل قبلی 5M فشرده باشند؛ رنج آن‌ها پیش‌فرض حداکثر `0.60%` باشد.
- کندل آخر، سقف همان ۸ کندل را با بافر `0.03%` بشکند.
- بدنه کندل شکست حداقل `55%` کل کندل باشد.
- کندل نزدیک سقف بسته شود.
- حجم کندل شکست حداقل `1.20x` میانگین حجم باشد.
- RSI بین `50` تا `64` باشد؛ نه ضعیف و نه خسته.
- MACD Histogram نسبت به کندل قبل بهتر شده باشد.
- حرکت ۳ کندل اخیر بیشتر از `0.75%` نباشد.
- اگر قیمت هم‌زمان از EMA50 و VWAP خیلی دور شده باشد، ورود رد می‌شود.

### قوانین ورود SHORT

- 4H و 1H نزولی باشند.
- ۸ کندل قبلی 5M فشرده باشند.
- کندل آخر، کف همان ۸ کندل را با بافر `0.03%` بشکند.
- بدنه کندل شکست حداقل `55%` کل کندل باشد.
- کندل نزدیک کف بسته شود.
- حجم کندل شکست حداقل `1.20x` میانگین حجم باشد.
- RSI بین `36` تا `50` باشد.
- MACD Histogram نسبت به کندل قبل ضعیف‌تر شده باشد.
- حرکت ۳ کندل اخیر بیشتر از `0.75%` نباشد.
- اگر قیمت هم‌زمان از EMA50 و VWAP خیلی دور شده باشد، ورود رد می‌شود.

## امتیازدهی

| بخش | امتیاز |
|---|---:|
| جهت 4H | 15 |
| جهت 1H | 15 |
| فشردگی ۸ کندل قبل | 12 |
| شکست معتبر محدوده فشرده | 18 |
| قدرت کندل شکست | 15 |
| حجم پشت شکست | 10 |
| RSI تازه و غیرخسته | 10 |
| MACD Histogram تازه در جهت معامله | 10 |
| ضد دیر ورود / حرکت ۳ کندل اخیر | 5 |
| جمع | 100 |

## فیلترهای رد سخت

ربات سیگنال صادر نمی‌کند اگر:

- 4H و 1H همسو نباشند.
- ۸ کندل قبلی فشردگی کافی نداشته باشند.
- شکست واقعی محدوده فشرده رخ نداده باشد.
- کندل شکست بدنه ضعیف داشته باشد.
- کندل شکست نزدیک سقف/کف بسته نشده باشد.
- حجم پشت شکست کافی نباشد.
- RSI در محدوده تازه و غیرخسته نباشد.
- MACD Histogram در جهت معامله تازه بهتر نشده باشد.
- حرکت ۳ کندل اخیر نشان دهد ورود دیر شده است.
- SL پنج دقیقه‌ای نامعتبر، خیلی کوچک یا خیلی بزرگ باشد.
- سود خالص بعد از کارمزد کمتر از حداقل پنل باشد.

## دستورات تلگرام

```text
ترید
ترید فعال
ترید خاموش
ترید دلار 10
ترید لوریج 10
حداکثر پوزیشن 3
سرمایه ترید 100
حداقل سود خالص 0.01
آمار
آمار 7
حذف آمار
اتو سیگنال
پوزیشن
کوین‌ها
وضعیت
راهنما
```

## دستور حذف آمار

در تلگرام بنویس:

```text
حذف آمار
```

این دستور آمار شمارشی، خطاها و رویدادهای مانیتورینگ قبلی را صفر می‌کند، ولی سیگنال‌های باز را نگه می‌دارد و سود/ضرر کل و سود/ضرر امروز را حفظ می‌کند.

## گزارش اتو سیگنال و دلیل رد شدن ارزها

دستور تلگرام:

```text
اتو سیگنال
```

این دستور آخرین چرخه اسکن را نشان می‌دهد:

- کل ارزهای لیست
- تعداد ارزهای اسکن‌شده
- تعداد سیگنال صادرشده
- تعداد ارزهای ردشده
- تعداد ارزهایی که به خاطر داشتن سیگنال باز دوباره اسکن نشدند
- تعداد خطاها و کول‌داون‌ها
- دلایل پرتکرار رد شدن
- آخرین ارزهای ردشده با دلیل

## مانیتورینگ و ثبت نتیجه

- لوپ مانیتورینگ جدا از لوپ اسکن اجرا می‌شود.
- هر سیگنال باز با قیمت OKX برای TP/SL چک می‌شود.
- نتیجه در جدول `signals` و `monitor_events` ثبت می‌شود.
- پیام نتیجه روی سیگنال اصلی ریپلای می‌شود.
- اگر ریپلای تلگرام به هر دلیل شکست بخورد، پیام نتیجه به‌صورت پیام عادی هم ارسال می‌شود تا نتیجه گم نشود.
- برای سیگنال Real، اگر Toobit نشان دهد پوزیشن بسته شده، نتیجه واقعی/خروج هم ثبت می‌شود.

## اجرای مستقیم روی VPS

پروژه فایل shell ندارد. اجرای سرویس باید مستقیم به `main.py` وصل باشد:

```ini
[Service]
WorkingDirectory=/root/forex-signal-bot
EnvironmentFile=/etc/forex-signal-bot.env
ExecStart=/usr/bin/python3 -u /root/forex-signal-bot/main.py
Restart=always
RestartSec=10
```

گیت‌پول و ریستارت بدون پاکسازی:

```bash
cd /root/forex-signal-bot || exit
git pull origin main
python3 -m py_compile *.py
sudo systemctl restart forex-signal-bot.service
sudo systemctl status forex-signal-bot.service --no-pager -l
journalctl -u forex-signal-bot.service -n 80 --no-pager
```

## فایل‌هایی که عمداً وجود ندارند

این فایل‌ها عمداً داخل پروژه نیستند تا برای Git pull و VPS دردسر نسازند:

- `.env.example`
- `.gitignore`
- `run.sh`
- `start_bot.sh`
- هر فایل `.sh`
- `__pycache__`
