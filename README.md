# Crypto 5M Simple Toobit Scalper

ربات اسکالپ ۵ دقیقه‌ای ساده برای تحلیل با OKX و اجرای واقعی روی Toobit.

## قوانین اصلی

- تحلیل فقط از OKX انجام می‌شود.
- اجرای واقعی فقط روی Toobit انجام می‌شود.
- پنل ترید، دستورات، مارجین، لوریج، آمار، سیگنال عادی/Real، اسلات‌ها و ثبت نتایج مثل نسخه قبلی حفظ شده‌اند.
- تایم‌فریم جهت: `4H + 1H` باید همسو باشند.
- تایم‌فریم ورود: `5m`.
- بدون تأیید کندلی؛ چون در اسکالپ ۵ دقیقه‌ای همان کندل می‌تواند کل سود باشد.
- بدون حمایت/مقاومت.
- بدون AI، DCA، Martingale و trailing.
- امتیاز از ۱۰۰؛ حداقل صدور سیگنال `70`.
- RR پیش‌فرض `1.5`.
- TP و SL فقط مخصوص ۵ دقیقه هستند.
- حداقل سود خالص بعد از کارمزد باید حداقل مقدار پنل باشد؛ پیش‌فرض `0.01 USDT`.

## ورود بهینه Pullback Re-Entry

نسخه فعلی به‌جای اینکه بعد از کامل شدن حرکت وارد شود، فقط وقتی ورود می‌دهد که قیمت در ناحیه سالم پولبک باشد:

- اول 4H و 1H جهت اصلی را مشخص می‌کنند.
- بازار 5M اگر رنج/کم‌قدرت باشد رد می‌شود.
- لانگ در اوج پامپ و شورت در ته دامپ رد می‌شود.
- قیمت باید نزدیک EMA50 یا VWAP باشد، نه خیلی چسبیده و نه خیلی دور.
- برای جلوگیری از نویز، فاصله باید حداقل `0.05%` و حداکثر `0.35%` از EMA50/VWAP باشد.
- RSI باید تازه و غیرخسته باشد.
- MACD Histogram باید در جهت معامله بهتر شده باشد.
- از ۳ تریگر ورود، حداقل ۲ تریگر باید فعال باشد.

تریگرهای ورود:

```text
1. برگشت سالم قیمت به سمت درست EMA50 یا VWAP
2. RSI تازه در جهت معامله ولی غیرخسته
3. MACD Histogram در جهت معامله بهتر شده
```

این منطق حمایت/مقاومت نیست؛ فقط جلوگیری از ورود دیر، ورود در رنج و ورود روی نویز است.

## امتیازدهی جدید

| بخش | امتیاز |
|---|---:|
| جهت 4H | 15 |
| جهت 1H | 15 |
| روند 5M با EMA/VWAP | 15 |
| کیفیت ورود Pullback Re-Entry | 20 |
| RSI تازه و غیرخسته | 15 |
| MACD/Momentum | 10 |
| ATR / کیفیت SL در 5M | 10 |
| جمع | 100 |

## فیلترهای رد سخت

ربات سیگنال صادر نمی‌کند اگر:

- 4H و 1H همسو نباشند.
- بازار 5M رنج/کم‌قدرت باشد.
- ورود لانگ بعد از پامپ و خستگی باشد.
- ورود شورت بعد از دامپ و خستگی باشد.
- قیمت هنوز به اندازه کافی از EMA50/VWAP فاصله نگرفته باشد و احتمال نویز باشد.
- قیمت خیلی از EMA50/VWAP دور شده باشد و ورود دیر باشد.
- RSI خارج از محدوده تازه و غیرخسته باشد.
- SL پنج دقیقه‌ای نامعتبر، خیلی کوچک یا خیلی بزرگ باشد.
- سود خالص بعد از کارمزد کمتر از حداقل پنل باشد.

## دستورات تلگرام

```text
ترید
ترید فعال
ترید خاموش
ترید دلار 10
ترید لوریج 10
حداکثر پوزیشن 3
سرمایه ترید 100
حداقل سود خالص 0.01
آمار
آمار 7
حذف آمار
اتو سیگنال
پوزیشن
کوین‌ها
وضعیت
راهنما
```

## دستور حذف آمار

در تلگرام بنویس:

```text
حذف آمار
```

این دستور آمار شمارشی، خطاها و رویدادهای مانیتورینگ قبلی را صفر می‌کند، ولی سیگنال‌های باز را نگه می‌دارد و سود/ضرر کل و سود/ضرر امروز را حفظ می‌کند.

## گزارش اتو سیگنال و دلیل رد شدن ارزها

دستور تلگرام:

```text
اتو سیگنال
```

این دستور آخرین چرخه اسکن را نشان می‌دهد:

- کل ارزهای لیست
- تعداد ارزهای اسکن‌شده
- تعداد سیگنال صادرشده
- تعداد ارزهای ردشده
- تعداد ارزهایی که به خاطر داشتن سیگنال باز دوباره اسکن نشدند
- تعداد خطاها و کول‌داون‌ها
- دلایل پرتکرار رد شدن
- آخرین ارزهای ردشده با دلیل

## مانیتورینگ و ثبت نتیجه

- لوپ مانیتورینگ جدا از لوپ اسکن اجرا می‌شود.
- هر سیگنال باز با قیمت OKX برای TP/SL چک می‌شود.
- نتیجه در جدول signals و monitor_events ثبت می‌شود.
- پیام نتیجه روی سیگنال اصلی ریپلای می‌شود.
- اگر ریپلای تلگرام به هر دلیل شکست بخورد، پیام نتیجه به‌صورت پیام عادی هم ارسال می‌شود تا نتیجه گم نشود.
- برای سیگنال Real، اگر Toobit نشان دهد پوزیشن بسته شده، نتیجه واقعی/خروج هم ثبت می‌شود.

## اجرای مستقیم روی VPS

پروژه فایل shell ندارد. اجرای سرویس باید مستقیم به `main.py` وصل باشد:

```ini
[Service]
WorkingDirectory=/root/forex-signal-bot
EnvironmentFile=/etc/forex-signal-bot.env
ExecStart=/usr/bin/python3 -u /root/forex-signal-bot/main.py
Restart=always
RestartSec=10
```

گیت‌پول و ریستارت بدون پاکسازی:

```bash
cd /root/forex-signal-bot || exit
git pull origin main
python3 -m py_compile *.py
sudo systemctl restart forex-signal-bot.service
sudo systemctl status forex-signal-bot.service --no-pager -l
journalctl -u forex-signal-bot.service -n 80 --no-pager
```

## فایل‌هایی که عمداً وجود ندارند

این فایل‌ها عمداً داخل پروژه نیستند تا برای Git pull و VPS دردسر نسازند:

- `.env.example`
- `.gitignore`
- `run.sh`
- `start_bot.sh`
- هر فایل `.sh`
- `__pycache__`
